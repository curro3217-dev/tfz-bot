# Caché de velas (se reconstruye sola en cada arranque; NO subir, son MB)
data_cache/
*.parquet

# Datasets, snapshots, logs y temporales (no van al repo)
*.csv
*.html
snapshots/
_*.py
_*.csv
_*.txt

# Estado/salidas en JSON (la cartera se gestiona aparte con -f en la nube)
backtest_summary.json
execution_state.json
*.json

# Logs
*.log
paper_log.txt

# Copias de seguridad
*.bak
*.bak-*
tfz_data.db.bak*

# Estado del paper: lo gestiona el workflow con `git add -f` en la nube,
# pero el de tu PC (con trades locales) NO debe subirse.
tfz_data.db
portfolio_state.json
weekend_paper.db
premium_paper.db
micro_data.db
micro_collector_log.txt
weekend_log.txt

# Python
__pycache__/
*.pyc
.venv/
venv/

# Modelos ML locales
*.joblib
