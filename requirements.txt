ccxt>=4.0
pandas>=2.0
numpy>=1.24
pyarrow>=12.0
requests>=2.31       # notify / scanner_bridge
urllib3>=2.0         # data_fetcher (disable_warnings)
scikit-learn==1.9.0  # Fase 2: filtro ML. FIJADO a la version del PC para que el modelo
                     # ml_model.joblib cargue igual en GitHub (evita mismatch de version)
joblib>=1.3          # persistencia del modelo (ml_model.joblib)
plotly>=5.15         # main.py importa snapshot.py (lo usa) -> necesario aunque no en la nube
arch>=6.0            # garch_sizing.py: forecast GARCH(1,1) para la linea de sizing en alertas
